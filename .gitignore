target/
dist/
build/
node_modules/
.tmp/
*.tgz
*.rpk

# Fixture golden 解包目录：仅供本地人审，每次重建变动巨大且不被任何自动化测试
# 消费（compat_goldens 只读 summary.json）。需要时通过 `astroforge test-compat
# --official` 本地重生。
fixtures/*/golden/*/unpacked/

# CLI 平台子包内的预编译二进制：CI release 流水线产出，仅在发布阶段 stage
# 进 bin/ 后 npm publish；本地手动发布也会 stage 一份，不应入库。
packages/cli-*/bin/astroforge
packages/cli-*/bin/astroforge.exe
.DS_Store
.idea/
.vscode/*
!.vscode/settings.json.example
*.log
.pnpm-debug.log*
.turbo/
.rsbuild/
coverage/
*.swp
.env
.env.local
